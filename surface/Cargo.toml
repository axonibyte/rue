[package]
name = "rue-surface"
description = "The .rue front end: lexer, parser with recovery over a lossless tree, formatter, and (from unit B) the resolver to the plan IR"
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rue-core = { path = "../core" }
logos.workspace = true
rowan.workspace = true
serde.workspace = true
toml.workspace = true
