worker on db-01: refused; E0609 at step 1: op start_worker: its computed undo acts on service:state:worker on db-01, reached by ssh(), which reads files alone, and no probe reads that fact (new in v0.3.0: declare a probe that `reads` the fact, or undo it with `:restore`).
