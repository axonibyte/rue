# T3's inventory with fw-win-01 declaring an artifact language that has no
# template on Windows: `sh` is POSIX only.
[[host]]
name = "fw-01"
address = "10.0.1.1"
os = "freebsd"
roles = ["fw"]
reach = ["ssh"]
filesystem = true
scheduler = "cron"

[[host]]
name = "fw-win-01"
address = "10.0.1.2"
os = "windows"
roles = ["fw"]
reach = ["ssh"]
filesystem = true
scheduler = "task-scheduler"
artifact = "sh"

[authenticators]
netops = { human = true }
