# Reverts: a secret still held when the instance reverts is dropped and journaled (5.13); here the revert leaves it in the daemon's memory.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/lifecycle.rs
+++ b/engine/src/lifecycle.rs
@@ -2035,7 +2035,6 @@
                 reason: reason.clone(),
             },
         )?;
-        self.drop_secrets(rec, &reason)?;
         self.release(rec)?;
         self.remove_dirs(rec)?;
         self.persist(rec)
