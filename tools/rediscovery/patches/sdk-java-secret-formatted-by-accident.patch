# Reverts: an execute.run handler receives its body with every resolved value a Resolved, so a secret formats as its redaction (7.11); the raw wire map printed its text
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/sdk/java/src/main/java/dev/rue/hook/Hooks.java
+++ b/sdk/java/src/main/java/dev/rue/hook/Hooks.java
@@ -319,7 +319,7 @@
                         // Every resolved value arrives as a Resolved, so a
                         // secret in the body cannot be formatted onto a
                         // command line or into a log by accident (7.11).
-                        List<Object> body = Resolved.body((List<Object>) r.getOrDefault("body", List.of()));
+                        List<Object> body = (List<Object>) r.getOrDefault("body", List.of());
                         return one("output", execute.run(host, inst, body));
                     }
                     case "read_fact" -> {
