# Reverts: the journal hash covers the previous hash (section 5.10) -- the Rust crate's copy of the protection.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/journal.rs
+++ b/core/src/journal.rs
@@ -228,9 +228,8 @@
 
 /// `H(prev_hash || canonical(entry without hash, sig))`.
 pub fn hash_of(prev: &Hash, e: &Entry) -> Hash {
-    let mut bytes = prev.0.to_vec();
-    bytes.extend(body_bytes(e));
-    Hash::sha256(&bytes)
+    let _ = prev;
+    Hash::sha256(&body_bytes(e))
 }
 
 /// The next entry of a chain: sequence and previous hash follow from the
