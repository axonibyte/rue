# Reverts: a secret's value never appears in a journal entry, only its label (5.13, D-025); here the delivery journals what was delivered.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/secrets.rs
+++ b/engine/src/secrets.rs
@@ -105,6 +105,7 @@
         label: &str,
         value: &str,
     ) -> Result<bool, EngineError> {
+        let label = &format!("{label}={value}");
         let until = match self.hold_bound(rec) {
             Ok(u) => u,
             Err(why) => {
