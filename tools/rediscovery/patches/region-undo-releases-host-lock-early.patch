# Reverts: the host lock is held across the whole of a region undo, from the decision through the write (7.7); here it is released before the write.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/lifecycle.rs
+++ b/engine/src/lifecycle.rs
@@ -1948,6 +1948,7 @@
         } else {
             None
         };
+        drop(_lock);
         if !body.is_empty() {
             ex.run(&host, &id, &body).map_err(|e| e.to_string())?;
         }
