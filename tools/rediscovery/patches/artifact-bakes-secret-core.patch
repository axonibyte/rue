# Reverts: a secret reference is never baked into an artifact (sections 5.8 and 7.7) -- the artifact is a file on the target, readable by whoever reads the instance directory.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/render/src/actions.rs
+++ b/render/src/actions.rs
@@ -130,7 +130,8 @@
         },
         // Observable on the target at run time, but an artifact runs no
         // probe: not bakeable this unit (recorded as not proven).
-        Ref::Fact(_) | Ref::Output { .. } | Ref::Controller(_) | Ref::Secret(_) => {
+        Ref::Secret(n) => Ok(n.clone()),
+        Ref::Fact(_) | Ref::Output { .. } | Ref::Controller(_) => {
             Err(RenderError::NotBakeable {
                 step,
                 what: format!("reference {}", r.label()),
