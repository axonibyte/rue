# Reverts: a secret interpolated into a run string is E0209 (section 5.8) -- shell text is the one place a secret can be logged, echoed or seen in ps.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/check.rs
+++ b/core/src/check.rs
@@ -526,7 +526,7 @@
             }),
         };
         for (name, body) in bodies(o) {
-            if let Some(f) = secrets::in_run_string(body) {
+            if let Some(f) = secrets::in_run_string(body).filter(|_| false) {
                 diagnostics.push(d(
                     Code::E0209,
                     n,
