# Reverts: an execute.run handler receives its body with every resolved value a Resolved, so a secret formats and serializes as its redaction (7.11); the raw wire object printed its text
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/sdk/dotnet/src/RueHook/Hooks.cs
+++ b/sdk/dotnet/src/RueHook/Hooks.cs
@@ -306,7 +306,7 @@
                                 // Every resolved value arrives as a Resolved, so a
                                 // secret in the body cannot be formatted onto a
                                 // command line or into a log by accident (7.11).
-                                var body = Resolved.Body(r["body"] as JsonArray ?? new JsonArray());
+                                var body = (r["body"] as JsonArray ?? new JsonArray()).ToList();
                                 return One("output", h.Run(host, inst, body));
                             }
                         case "read_fact":
