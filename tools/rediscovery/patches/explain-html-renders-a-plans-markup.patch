# Reverts: the HTML page escaping a plan's own text; here an op named <script> becomes markup
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/explain.rs
+++ b/core/src/explain.rs
@@ -408,14 +408,7 @@
 fn escape(s: &str) -> String {
     let mut out = String::with_capacity(s.len());
     for c in s.chars() {
-        match c {
-            '&' => out.push_str("&amp;"),
-            '<' => out.push_str("&lt;"),
-            '>' => out.push_str("&gt;"),
-            '"' => out.push_str("&quot;"),
-            '\'' => out.push_str("&#39;"),
-            _ => out.push(c),
-        }
+        out.push(c);
     }
     out
 }
