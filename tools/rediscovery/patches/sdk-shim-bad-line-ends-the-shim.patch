# Reverts: a line that is not JSON is skipped by the rue-hook shim, as every SDK skips it; returned as an error it ended the shim and every later request met silence
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/sdk/shim/src/main.rs
+++ b/sdk/shim/src/main.rs
@@ -107,15 +107,7 @@
         }
     }
 
-    loop {
-        let frame = match read_frame(&mut r) {
-            Ok(Some(frame)) => frame,
-            Ok(None) => break,
-            // A line that is not JSON is skipped like any other line that
-            // is not a request; returned as an error, it ended the shim.
-            Err(e) if e.kind() == std::io::ErrorKind::InvalidData => continue,
-            Err(e) => return Err(e),
-        };
+    while let Some(frame) = read_frame(&mut r)? {
         if frame.is_null() || frame.get("kind").is_none() {
             continue;
         }
