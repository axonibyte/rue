# Reverts: a secret referenced from a :target undo body is E0210 (section 5.8) -- the artifact would have to carry the secret to the target.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/check.rs
+++ b/core/src/check.rs
@@ -503,7 +503,7 @@
             }
         }
         if o.undo_locus == UndoLocus::Target {
-            if let Some(f) = undo_body(o).and_then(secrets::anywhere) {
+            if let Some(f) = undo_body(o).and_then(secrets::anywhere).filter(|_| false) {
                 diagnostics.push(d(
                     Code::E0210,
                     n,
