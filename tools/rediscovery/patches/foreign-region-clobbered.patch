# Reverts: a region's damaged-marker fallback is conditional on no foreign region (section 5.2, D-053)
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/proto/src/Rue/Proto/Check.hs
+++ b/proto/src/Rue/Proto/Check.hs
@@ -234,6 +234,7 @@
       , effectiveDrift o == Just Clobber
       , e <- opFootprint o
       , fpKind e == Region
+      , False
       ]
     controllerOnly = [n | (n, o) <- stepOps, opUndoLocus o == UndoController]
     -- Section 8.2: "post-fence steps held indefinitely until an operator
