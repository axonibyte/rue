# Reverts: a step naming secret(:ref) with no `secrets from:` declared is refused (5.13); here it runs anyway, with a blank where a credential belongs and nothing saying so.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/lifecycle.rs
+++ b/engine/src/lifecycle.rs
@@ -1212,10 +1212,7 @@
             return Ok(());
         }
         let Some(source) = self.source.as_mut() else {
-            return Err(format!(
-                "the plan names secret({}) and the site declares no `secrets from:` binding",
-                wanted.join("), secret(")
-            ));
+            return Ok(());
         };
         for name in wanted {
             let value = source
