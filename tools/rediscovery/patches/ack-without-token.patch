# Reverts: a knell's acknowledgement is a proof in its own scope, verified by the approval binding (5.11, 6.8); here a reason alone fires the knell.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/gates.rs
+++ b/engine/src/gates.rs
@@ -525,19 +525,7 @@
                     reason: "the site declares no approval binding".into(),
                 },
             };
-            if !verdict.verified {
-                self.log(
-                    &rec,
-                    J::Denied {
-                        gate: format!("ack {step}"),
-                        reason: verdict.reason.clone(),
-                    },
-                )?;
-                return Err(EngineError::Runtime(format!(
-                    "the acknowledgement from {authenticator} was not accepted: {}",
-                    verdict.reason
-                )));
-            }
+            let _ = &verdict;
             let mut proofs = self.proofs_for(&rec, Scope::Ack(step));
             proofs.push(authenticator.to_string());
             let auths = self.authenticators();
