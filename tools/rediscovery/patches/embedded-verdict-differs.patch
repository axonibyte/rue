# Reverts: the IR an embedded host sends carries everything check reads (Phase 4's acceptance line: the same text checks identically standalone and embedded); here the probe declarations are left out of it, so the daemon checks a different plan from the one the person checked.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/model.rs
+++ b/core/src/model.rs
@@ -565,7 +565,7 @@
     pub body: Vec<Item>,
     /// The probes the plan's guards, observes and asserts may name (IR 4),
     /// and the probes that read its non-file facts (IR 5).
-    #[serde(default)]
+    #[serde(default, skip_serializing)]
     pub probes: Vec<ProbeDecl>,
 }
 
