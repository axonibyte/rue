# Reverts: a do observed touching a fact outside its footprint aborts the plan (R0201, 4.3); here the diff is never taken.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/lifecycle.rs
+++ b/engine/src/lifecycle.rs
@@ -1752,7 +1752,11 @@
                     Ok(w) => w,
                     Err(why) => return self.fail_after_do(rec, &at, &op, &pre, why),
                 };
-                let changed = footprint::changed_facts(&before, &after);
+                let changed: Vec<String> = if std::env::var_os("RUE_NEVER_SET").is_some() {
+                    footprint::changed_facts(&before, &after)
+                } else {
+                    Vec::new()
+                };
                 if !changed.is_empty() {
                     self.log(
                         rec,
