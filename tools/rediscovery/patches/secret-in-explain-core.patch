# Reverts: explain marks a secret reference as <secret:label> (section 5.8) -- the rendering is the only redaction the listing has.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/core/src/explain.rs
+++ b/core/src/explain.rs
@@ -238,11 +238,7 @@
 }
 
 fn ref_text(r: &Ref) -> String {
-    if r.is_secret() {
-        format!("<secret:{}>", r.label())
-    } else {
-        format!("#{{{}}}", r.label())
-    }
+    format!("#{{{}}}", r.label())
 }
 
 fn cost_text(r: &Refusal) -> String {
