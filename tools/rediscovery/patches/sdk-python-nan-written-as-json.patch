# Reverts: a NaN or an infinity in a reply is refused rather than written as a bare NaN or Infinity, a line the engine cannot parse
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/sdk/python/rue_hook/serve.py
+++ b/sdk/python/rue_hook/serve.py
@@ -46,7 +46,7 @@
     # parse reads as a silence. Flushed every time: an unflushed reply is a
     # silence, and silence is a refusal of the step with nothing to tell the
     # operator.
-    text = json.dumps(frame, default=_encode, allow_nan=False)
+    text = json.dumps(frame, default=_encode)
     out.write(text + "\n")
     out.flush()
 
