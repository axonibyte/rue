# Reverts: secret(:x) is a Secret reference (section 5.13) -- classified as a parameter it would bake into a run string, an artifact, or a listing unredacted.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/surface/src/resolve/expand.rs
+++ b/surface/src/resolve/expand.rs
@@ -1465,7 +1465,7 @@
                     _ => None,
                 });
                 match name {
-                    Some(n) => Some(b::secret(&n)),
+                    Some(n) => Some(b::param(&n)),
                     None => {
                         diags.push(self.err(
                             cx.module,
