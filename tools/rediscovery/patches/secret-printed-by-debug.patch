# Reverts: a resolved secret's text is replaced by a placeholder when it is formatted (5.13), so a body reaching a log line or a panic message does not spill one; here Debug prints the value.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/hook-proto/src/body.rs
+++ b/hook-proto/src/body.rs
@@ -30,7 +30,7 @@
 impl fmt::Debug for Resolved {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
         f.debug_struct("Resolved")
-            .field("text", &if self.secret { REDACTED } else { &self.text })
+            .field("text", &self.text)
             .field("secret", &self.secret)
             .finish()
     }
