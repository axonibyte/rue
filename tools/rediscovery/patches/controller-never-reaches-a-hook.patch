# Reverts: the controller is reached by a `transport: :controller` hook when the site binds one; here it is local() alone, as it was, so every :controller step with a hook action -- T2's fence, gate and succession record -- goes to an executor that refuses it.
# Rediscovered by the selector named in tools/rediscovery/table.tsv.
--- a/engine/src/lifecycle.rs
+++ b/engine/src/lifecycle.rs
@@ -777,7 +777,7 @@
             // A `transport: :controller` hook, when the site binds one, and
             // `local()` otherwise: the same list the checker's E0608 reads,
             // so a plan it passes is one this can place.
-            rue_core::check::CONTROLLER_REACH.to_vec()
+            vec!["local"]
         } else {
             host.record.reach.iter().map(String::as_str).collect()
         };
